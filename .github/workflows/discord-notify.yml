name: Notify Discord

on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Discord Embed
        run: |
          curl -H "Content-Type: application/json" \
          -X POST \
          -d '{
            "embeds": [
              {
                "title": "🤖 Case Bot Updated",
                "description": "บอทนับเคสมีการอัปเดตเรียบร้อย",
                "color": 3066993,
                "fields": [
                  {
                    "name": "Repository",
                    "value": "'"${{ github.repository }}"'",
                    "inline": true
                  },
                  {
                    "name": "Branch",
                    "value": "'"${{ github.ref_name }}"'",
                    "inline": true
                  },
                  {
                    "name": "By",
                    "value": "'"${{ github.actor }}"'",
                    "inline": true
                  }
                ],
                "footer": {
                  "text": "Commit: ${{ github.sha }}"
                }
              }
            ]
          }' \
          ${{ secrets.DISCORD_WEBHOOK }}
